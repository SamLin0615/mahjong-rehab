version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WDS_SOCKET_PORT=0 # Fixes some hot-reload issues in Docker